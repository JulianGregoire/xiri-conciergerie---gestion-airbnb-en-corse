---
import { Image } from "astro:assets";
import { servicesData } from "../data/servicesData";

import img01 from "../images/gestion_annonces_airbnb.png";
import img02 from "../images/selection_des_voyageurs.png";
import img03 from "../images/checkin_checkout.png";
import img04 from "../images/menagee.png";
import img05 from "../images/assurances_cautions.png";
import img06 from "../images/maintenance_assistance.png";

const services = Object.values(servicesData);

// Icon mapping to SVG components or imported Images
const icons: Record<string, any> = {
    "01": img01,
    "02": img02,
    "03": img03,
    "04": img04,
    "05": img05,
    "06": img06,
};
---

<section id="services" class="py-24 md:py-32 lg:py-48 relative overflow-hidden">
    <div class="max-w-7xl mx-auto relative z-10 px-6 lg:px-12">
        <div
            class="mb-10 md:mb-12 flex flex-col md:flex-row md:items-end justify-between gap-5"
        >
            <div class="max-w-3xl">
                <span
                    class="text-xiri-gold text-[11px] font-bold uppercase tracking-[0.25em] mb-3 block"
                    >NOS SERVICES TOUT-COMPRIS</span
                >
                <h2
                    class="text-[30px] md:text-[34px] font-serif text-xiri-navy leading-[1.2]"
                >
                    Nos services : Zéro logistique. <br />100% Sérénité.
                </h2>
            </div>
            <a
                href="#contact"
                class="bg-xiri-navy text-white px-6 py-3 text-[11px] uppercase tracking-[0.2em] font-extrabold hover:bg-xiri-gold transition-all duration-300 rounded-xl no-underline inline-block"
            >
                Demander un audit
            </a>
        </div>

        <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-5"
        >
            {
                Object.entries(servicesData).map(([key, service]) => (
                    <a 
                        href={`/services/${key}`}
                        class="bg-white border border-xiri-navy/10 p-8 md:p-10 h-full flex flex-col items-center text-center rounded-[32px] shadow-sm hover:shadow-xl hover:border-xiri-gold/30 hover:-translate-y-1 transition-all duration-500 no-underline group"
                    >
                        <div class="flex flex-col items-center gap-6 mb-6">
                            <div class="flex items-center justify-center flex-shrink-0 transition-transform duration-500 group-hover:scale-110">
                                {typeof icons[service.id] === 'string' ? (
                                    <div class="w-10 h-10 bg-xiri-bg flex items-center justify-center text-xiri-gold rounded-xl">
                                        <Fragment set:html={icons[service.id]} />
                                    </div>
                                ) : (
                                    <Image 
                                        src={icons[service.id]} 
                                        alt={service.title} 
                                        class={`${
                                            service.id === '01' ? 'w-20 h-20 sm:w-24 sm:h-24' : 
                                            service.id === '06' ? 'w-16 h-16 sm:w-18 sm:h-18' : 
                                            'w-18 h-18 sm:w-20 sm:h-20'
                                        } object-contain`} 
                                    />
                                )}
                            </div>
                            <h3 class="text-2xl md:text-3xl font-serif text-xiri-navy leading-tight group-hover:text-xiri-gold transition-colors duration-300">
                                {service.title}
                            </h3>
                        </div>

                        <p class="text-[15px] md:text-[16px] text-xiri-navy/70 font-medium leading-loose max-w-[280px] mb-8">
                            {service.description}
                        </p>

                        <div class="mt-auto pt-6 border-t border-xiri-navy/5 w-full flex items-center justify-center gap-3">
                            <div class="w-1.5 h-1.5 rounded-full bg-xiri-gold animate-pulse"></div>
                            <span class="text-[11px] font-black uppercase tracking-[0.2em] text-xiri-gold">
                                {service.benefit}
                            </span>
                        </div>
                    </a>
                ))
            }
        </div>
    </div>
</section>
